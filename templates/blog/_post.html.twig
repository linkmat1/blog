
<article class="blog-post">
    <div class="blog-post__body">
        <h2 class="blog-post__title"><a href="#">{{ post.title }}</a></h2>
        <div class="blog-post__meta">
            <div>{{ post.createdAt | date }}</div>
            <div>Auteur {{ post.author.username }}</div>
        </div>
        <p>
            {{ post.content  }}
        </p>
    </div>
</article>